{
  "name": "multipass",
  "version": "0.12.0",
  "description": "Concurrent multi-lens code review. Each review skill runs as a dedicated agent with its own context window and persistent project memory."
}
